// Declaração da variável constante botao para representar os elementos que devem ser lidos
const botao = document.querySelectorAll('.botao');

// Verificar o progresso inserindo console.log(botao)
console.log(botao);
